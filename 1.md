1) Заходим на лист ВВОД_CONST
2) смещаемся в графу F
* для понимания, только с 5 строчки начинаются нужные нам значения
3) в 5 строчки (в графе F) система смотрит на шифр (имя шифра)
4) система сохраняет его в память, создает новый лист на новой книге (Z) и вставляет забранный шифр во вторую строчку в графу D
5) теперь, система запоминает вложенный на новом листе шифр, возвращается в графу F в то место, откуда она его скопировала, проверяет, соответствует ли найденный шифр в этой же строчки, тому что мы вложили на лист, если да -  смотрит в графу N 
*в данной графе нет значений, там находится результат отработки формулы (иначе, просто формул)
6) как только система нашла аналогичный шифр начинается цикл - из графы N соответсвующей горизонтали, система запоминает или сохраняет найденное значение (в виде значения а не формулы, как лучше перевести - подскажи),
7) движется вниз в графе F, проверяет на совпадение шифр, прибавляет к первому значению из N строчки новое значение
иначе говоря: двигайся по графе F и ищи совпадение с тем шифров, который мы перенесли на новый лист - нашел, сохрани значение из графы N и проверяй дальше, нашел -снова прибавь (учти что шифры могут НЕ идти друг за другом, строк 6000, наша задаа с минимальной нагрузкой осуществить такую проверку и накопительную сумму),
8) как только все шифры проверены и была собрана накопительная сумма по первому шифру, иди на новый лист (где уже сидит шифр) в соседнюю ячейку вставь сумму, всю гданную графу можно сразу отформатировать и сделать там формат чисел с 2 запятыми
9) как только вставили значение, вернись в графу F на листе ВВОД_CONST, ищи первый - не похожий на перенесенный шифр на новом листе и выполни все процедуры с ним
10) выполняй пока не "вырежем" все существвюущие шифры и не накопим все суммы
11) на новом листе у нас должен сформироваться список из шифров, рядом результат наколпения сумм
12) все возможные операции с копировать - вставить выполняй через значение
13) при работе макроса с данными примени все необходимые операции по работе с "грязными данными"


Внесем корректировки в цикл
в моменте, когда мы вытащили шифр, собрали накопительную сумму, перед тем как мы отправимся за новым шифров, подключим перечень команд, а именно
1) как только перенесли шифр и собрали сумму, уходим на лист DECADA, прыгаем в графу C, , в данной графе у нас вновь начинаются шифры
2) наша задача, проверять шифр в графе С, запускаем вновь условие проверки, если шифр такой же, как на вынесенном листе, идем  в графу D, E, F, G, H, I, K, хватаем эти значения и переносим под наш шифр на новом листе (по структуре получаем, шифр, справа от шифра сумму, снизу от шифра значение из графы D листа DECADA, рядом, по порядку соседства, вставляются значения D E F G H I K,
3) как только получили данные по шифру, начинаем все по новой - переносим шифр, копиим сумму, ищем шифр на декаде, переносим соответсвющие значения 


Модернизируем:

в моменте цикла, где у нас перенесся шифр, была накоплена сумма рядом, и перенесены строки с соответсвующими значениями из заданных графах подключаем нвоый блок действий:
1) теперь, мы движемся по графе D нового листа и сравниваем значение со значением на листе SILENT_ENGINE в графе 4 (учти что значения на этом листе в этой графе в виде формулы ВПР, если нашли совпадение, на этом же листе идем в графу E F G H I, хватаем значения, уходим обратно на новый лист в графу D, и вставляем значения E F G H I ( в том же порядке, только после Е остется пустая графа), учти что данные графы Е должны оказаться в графе D нового листа, а все соседние рядом (с учетомпробела) таким образом у нас формируется структура - шифр, под шифров список строк, забранных в предыдущей версии, но теперь у каждой такой строки свой новий список, у каждого списка должна быть своя группа для разворота 
